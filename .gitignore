node_modules
dist
dist-ssr
*.local
# Pega TUDO que começa com .env e libera só o template — antes `.env.*.local`
# não cobria `.env.production`/`.env.development`, que iriam pro git com segredo.
.env*
!.env.example
.DS_Store
.vercel
*.log

# Backup do C2S: contem nome/telefone/e-mail/conversas de clientes reais.
# Nunca versionar dados pessoais.
backup-c2s/

